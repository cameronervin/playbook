
Implement a feature using the implement-feature skill.

Follow the skill at `.claude/skills/implement-feature/SKILL.md` which includes:
- Preflight context gathering (read docs, PRD, identify affected layers)
- Execution planning for complex features
- TDD cycle (write failing test → minimal implementation → refactor)
- Documentation updates
- Verification and lint checks

Default prompt: Implement the requested feature following TDD principles. Write a failing test first, implement minimal code to pass, then refactor while keeping tests green.
