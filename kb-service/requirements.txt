# KB Service dependencies
# Python 3.12+

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
pgvector>=0.3

# Settings / validation
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# Task queue
celery[redis]>=5.4
redis>=5.0.0

# Logging
structlog>=24.0

# HTTP client (outbound webhook)
httpx>=0.27

# AWS / S3
boto3>=1.34
aiofiles>=24.1.0

# NLP / Embeddings
tiktoken>=0.7
langchain-text-splitters>=0.3

# Embedding provider (direct OpenAI or LiteLLM gateway — both via the OpenAI SDK)
openai>=1.40

# Parsers (OCR is a stub in this scaffold — no Textract / VLM deps)
docling>=1.16.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0
pymupdf>=1.24

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
