# Phase 2: Athlete AI Experience

## Scope Boundary For This Phase

Phase 2 builds the core athlete-facing AI loop: persisted conversation history,
message submission, streamed assistant responses, grounded KB retrieval,
citations, conversation-scoped file context, and safety/refusal behavior.

Streaming must use an async worker handoff: the message submit API persists the
user turn, creates an assistant placeholder, dispatches a Celery task to execute
the athlete chat agent, and returns a `task_id` plus stream metadata. The Celery
worker runs the LangGraph agent and publishes ordered chunks, progress,
completion, and error events to Valkey Streams, with a pub/sub notification path
for the HTTP stream endpoint. The stream endpoint validates the athlete's
conversation/message ownership and subscribes to the Valkey stream/channel for
the returned `task_id`.

The backend now includes reusable streaming infrastructure under
`backend/app/infrastructure/streaming/` plus `AgentStreamService`. The
message-submit and stream endpoints are wired, and the athlete Celery task now
executes the first Playbook athlete chat agent slice. A live local smoke now
confirms message submit -> Celery worker -> LangGraph execution -> Valkey SSE
progress/chunk/complete events -> assistant persistence for the unsupported
no-source path. Remaining Phase 2 AI work is mostly context engineering,
grounded citation quality, conversation file context, frontend integration, and
eval breadth.

| Status | Build Area | What To Build | Current Implementation | Validation | Source Specs |
|--------|------------|---------------|------------------------|------------|--------------|
| ☑ | Conversation history APIs | Finish create/list/detail conversation history with messages, citations, and file summaries scoped to the current athlete. | Conversation list/create/detail routes, service, schemas, and repositories exist for conversations/messages/citations/files. Create stores the initial message and returns an empty file list; detail returns bounded messages with citations plus safe top-level file summaries and chunk counts. | `POST /api/v1/conversations` creates conversation from initial message -> list returns only athlete-owned rows -> detail returns messages/citations/files -> cross-user access returns 403/404. | `prd/02-technical-docs/01-playbook/api-specification.md`, `prd/02-technical-docs/01-playbook/security.md` |
| ☑ | Message submit API | Add `POST /api/v1/conversations/{conversation_id}/messages` to persist the user turn, create an assistant placeholder, dispatch a Celery agent task, and return stream metadata. | Message submit route, schemas, service orchestration, file-id scoping, and Celery dispatch wrapper exist. The route commits the user message plus streaming assistant placeholder, binds a pre-generated `task_id` in metadata, dispatches the athlete chat task, and returns stream metadata. | POST persists user message -> assistant placeholder has streaming/pending status -> Celery task is enqueued with `task_id` -> response includes `user_message_id`, `assistant_message_id`, `task_id`, `stream_url`, and `status`. | `prd/02-technical-docs/01-playbook/api-specification.md`, `prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | Stream endpoint | Add `GET /api/v1/conversations/{conversation_id}/messages/{message_id}/stream` to stream assistant events for the returned `task_id` by subscribing to Valkey Streams/pub-sub events produced by the Celery worker. | Athlete SSE stream route validates conversation/message ownership plus `task_id` binding; `AgentStreamService` formats durable stream records as SSE and maps LangGraph `messages`, `custom`, and `updates` parts; `ValkeyAgentStreamProvider` continuously reads ordered events until terminal events; the athlete task publishes real progress/chunk/complete/error events. A live local smoke confirmed ordered progress events, an answer chunk, a terminal `complete`, and assistant persistence from worker-published Valkey records. | Route/unit tests cover ownership, cursor resume, SSE formatting, Valkey provider behavior with mocks, LangGraph mapping, and worker task publishing. Live backend + worker + Valkey smoke confirms submit -> worker publish -> SSE progress/chunk/complete -> terminal close. Remaining hardening: disconnect/unsubscribe smoke and broader operational resiliency. | `prd/02-technical-docs/01-playbook/api-specification.md`, `prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | LangGraph athlete chat graph | Build the athlete chat graph/executor to run inside a Celery worker with conversation ID as thread ID, bounded history, topic/risk labeling, retrieval, generation, persistence, and Valkey stream event publishing. | Athlete-specific state, structured output, load/safety/agent/save nodes, graph topology, and graph-builder compilation now replace the example workflow; `AthleteChatExecutor` delegates to the compiled graph and handles task failure marking. Live local smoke confirmed the worker executes the compiled graph with LiteLLM, emits lifecycle events, persists the final assistant message, and completes the stream for unsupported answers. Context engineering and answer quality tuning remain under the KB/eval rows. | Worker executes graph async with persisted checkpoint -> bounded history is injected -> topic/risk labels persist -> chunks and lifecycle events publish to Valkey under `task_id` -> graph can complete or fail safely by conversation/message/task ID. | `prd/02-technical-docs/01-playbook/agentic-framework.md`, `prd/02-technical-docs/01-playbook/integration-spec.md` |
| ◐ | KB-grounded answer generation | Retrieve ready visible KB context, generate concise answers, and persist citations from retrieved chunks. | Reusable profile-based KB tool factory exists and athlete chat binds `search_playbook_knowledgebase`; retrieved chunks are source-keyed for structured citation output; KB-service/local provider results now carry citation-ready Playbook document ID, KB-service document ID, stable chunk ID/index, score, and source metadata; `save_state` persists valid citation metadata to `message_citations`; unsupported KB-required answers are converted to refusals. The live smoke confirmed this no-source/unsupported path streams and persists. Remaining validation: context engineering plus golden retrieval/citations against live kb-service data. | NIL/compliance golden question retrieves expected source -> final answer includes citations -> citation document/chunk metadata maps to retrieved result -> zero-hit policy/process prompt declines and persists/streams as unsupported. | `prd/02-technical-docs/01-playbook/agentic-framework.md`, `prd/02-technical-docs/02-kb-service/retrieval.md` |
| ◐ | Safety and refusal policy | Implement unsupported, no-source, recruiting, medical/legal/mental-health, harassment/reporting, and emergency handling. | Deterministic athlete safety pre-checks cover emergency, medical, legal, mental-health, NIL, compliance, recruiting, harassment/reporting, and process labeling; emergency/sensitive prompts bypass the agent; no-source KB-required answers persist as unsupported refusals. Live smoke confirmed unsupported refusal stream/persistence for a KB-required prompt without adequate source support. Remaining validation: broader golden safety/eval set. | Emergency prompt refuses advice and shows emergency instructions -> unsupported NIL/compliance prompt declines -> sensitive prompts decline -> safety outcome and answer type persist on message metadata. | `prd/02-technical-docs/01-playbook/security.md`, `prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☐ | Conversation file upload | Add `POST /api/v1/conversations/{conversation_id}/files`, original file storage, extraction, chunking, and conversation-scoped context assembly. | Conversation file/chunk tables exist; storage provider exists. No API, repository, extraction service, or context assembler exists for athlete files. | Upload stores original -> extraction status moves uploaded/extracting/ready/failed -> chunks are available only to owning conversation -> shared KB search excludes athlete uploads. | `prd/02-technical-docs/01-playbook/data-model.md`, `prd/02-technical-docs/01-playbook/integration-spec.md` |
| ☐ | Athlete chat frontend | Build authenticated chat UI with conversation sidebar, message composer, streaming transcript, citation display, upload control, loading/error states, and responsive layout. | Frontend still renders scaffold home page and `ExampleList`; no auth/profile/chat routes are implemented. | Athlete opens chat -> can create/select conversation -> sends prompt and sees streamed response -> citations render below answer -> upload control reflects file status -> responsive layout remains usable. | `prd/01-user-stories/epic-2-athlete-ai-chat-experience.md`, `.claude/rules/13-frontend-design-standards.md`, `.claude/style/ui-patterns.md` |
| ◐ | Tests and eval smoke | Add service/API/frontend tests plus early golden smoke checks for grounded answers and refusals. | Backend repository, route, stream-service, worker, graph-builder, and executor tests exist; a live local smoke now proves submit -> worker -> graph -> SSE -> persistence for an unsupported answer. Frontend chat tests, file upload tests, live grounded KB citation golden cases, and broader eval sets remain. | Route tests cover ownership and stream behavior -> live smoke covers the asynchronous agent streaming loop -> frontend chat tests pass -> retrieval/refusal smoke cases pass against seeded/fake providers. | `prd/02-technical-docs/01-playbook/eval-framework.md`, `.claude/rules/06-testing.md` |

## Definition of Done

- [ ] Athletes can create, revisit, and continue conversations.
- [x] User messages submit through the API, enqueue a Celery agent task, return `task_id`, and assistant responses stream from Valkey Streams/pub-sub before persisting correctly.
- [ ] Grounded answers include citations tied to retrieved KB chunks.
- [ ] Unsupported and sensitive prompts decline safely.
- [ ] Conversation file uploads work without adding files to the shared KB.
- [ ] Athlete chat UI handles loading, streaming, citations, errors, and responsive layouts.
- [ ] Phase 2 tests and relevant eval smoke checks pass.
