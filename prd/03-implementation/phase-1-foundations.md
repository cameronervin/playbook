# Phase 1: Playbook Foundations

## Scope Boundary For This Phase

Phase 1 establishes the product-specific foundation for Playbook while
preserving reusable scaffold infrastructure. It should leave the repo ready for
Phase 2 athlete AI work, with real auth, roles, data models, auditability,
baseline KB document control-plane APIs, and frontend route shells.

This phase does **not** implement full AI answer quality, streamed chat,
dashboard insight generation, admin chat, or the complete KB admin experience.
Those come in later phases.

| Status | Build Area | What To Build | Current Implementation | Validation | Source Specs |
|--------|------------|---------------|------------------------|------------|--------------|
| ◐ | DB models and migrations | Replace scaffold data model with Playbook `organizations`, `users`, OAuth accounts, conversations/messages/citations/files, KB documents/events, analytics/admin chat, and audit tables. | SQLAlchemy models and Alembic migrations exist, including auth compatibility fields. Backend metadata/repository tests pass; live Docker-backed upgrade/downgrade still needs to be run. | Offline/live Alembic upgrade and downgrade pass -> metadata tests verify tables/defaults/indexes/constraints -> docs list current product tables. | `prd/02-technical-docs/01-playbook/data-model.md`, `docs/architecture/db.md` |
| ◐ | Auth/session services and APIs | Implement Google/Microsoft OAuth login, callback user upsert, app JWT/cookie session, provider listing, and logout. | `AuthService` and `/api/v1/auth/*` routes exist with OAuth state cookie, provider enablement, user upsert, app token creation, and logout. Additional route-level tests and configured-provider smoke checks are still needed. | Provider listing works -> fake Google/Microsoft callbacks create/update local users -> app session cookie set/cleared -> OAuth provider tokens are not returned or logged. | `prd/02-technical-docs/01-playbook/api-specification.md`, `prd/02-technical-docs/01-playbook/security.md` |
| ◐ | Athlete profile completion | Let first-time athlete users complete name and sport/team, then continue directly to chat. | `/api/v1/users/me` and `/api/v1/users/me/profile` exist; `UserProfileService` updates athlete profile and returns `/chat`. Frontend profile route is not built. | Signed-in athlete submits profile -> `profile_complete` becomes true -> response `next_route` is `/chat` -> missing/invalid fields return structured validation errors. | `prd/01-user-stories/epic-1-authentication-roles-and-seamless-entry.md` |
| ◐ | RBAC and admin user management | Enforce athlete/admin/super_admin dependencies and super-admin role management APIs. | Role dependencies exist; `/api/v1/admin/users` list and role update routes exist; role changes sync `is_superuser` and record audit events. Frontend route guards/admin shell are not built. | Athlete admin API returns 403 -> admin cannot call super-admin user APIs -> super_admin role update succeeds -> role change writes an audit record. | `prd/02-technical-docs/01-playbook/security.md`, `prd/02-technical-docs/01-playbook/api-specification.md` |
| ◐ | Audit logging | Add immutable audit creation/query support for role changes and foundational admin actions. | `AuditLog` model, repository, service, and `/api/v1/admin/audit-logs` route exist; role changes and KB document actions call audit service. More route-level and immutability tests are needed. | Role change audit exists -> KB upload/metadata/retry/delete audits exist -> super_admin can query audit logs -> no normal API mutates or deletes audit rows. | `prd/02-technical-docs/01-playbook/data-model.md`, `prd/02-technical-docs/01-playbook/security.md` |
| ◐ | KB document backend control plane | Create backend records and service contract for admin-uploaded document status before full admin UI. | `/api/v1/admin/kb/documents` routes and service exist for list/upload/get/metadata/retry/delete; signed `/api/v1/kb/webhook` status handling exists. Upload currently exposes title/source date/official/priority form fields but not PRD `metadata_tags` form input. | Admin upload creates document and audit event -> webhook maps processing/ready/failed -> metadata patch persists tags/ranking fields -> retry calls KB provider -> delete removes original and KB vectors. | `prd/02-technical-docs/01-playbook/data-model.md`, `prd/02-technical-docs/02-kb-service/api-contracts.md` |
| ◐ | Conversation shell backend | Create minimal athlete-owned conversation list/create/detail before AI message generation. | `/api/v1/conversations` list/create/detail routes exist; conversation, message, and citation repositories exist. Message submit, streaming, and file upload are Phase 2. | Athlete creates conversation -> list returns only owned conversations -> detail returns bounded messages/citations -> cross-athlete access returns 404/403. | `prd/02-technical-docs/01-playbook/api-specification.md`, `prd/01-user-stories/epic-2-athlete-ai-chat-experience.md` |
| ☐ | Frontend product identity and route shells | Replace scaffold `Example` UI with Playbook-neutral product copy and create route shells for login/profile/chat/admin. | Frontend still renders `Agentic App Scaffold` and `ExampleList`; scaffold metadata/test remain. | Primary route no longer shows scaffold copy -> `/profile`, `/chat`, and `/admin/*` shells exist -> no explicit university affiliation claims appear -> responsive shells render cleanly. | `prd/01-user-stories/epic-2-athlete-ai-chat-experience.md`, `prd/01-user-stories/epic-5-safety-governance-and-release-readiness.md`, `.claude/style/design-tokens.md`, `.claude/style/ui-patterns.md` |
| ◐ | Infrastructure and observability baseline | Wire DB/session, LLM provider, KB provider, storage provider, checkpointer, request context, CORS, error handlers, and structured logging. | Backend app startup wires these providers; request ID/error contract tests pass. Logs exist for auth, role changes, KB docs, and webhook events. Further no-token/no-secret log checks are needed. | Startup initializes providers -> request IDs appear in responses/log context -> errors use nested contract -> auth/admin logs omit OAuth tokens and secrets. | `prd/02-technical-docs/01-playbook/integration-spec.md`, `prd/02-technical-docs/01-playbook/agentic-framework.md`, `prd/02-technical-docs/01-playbook/security.md` |
| ◐ | Tests and docs | Add route-level coverage, verify live migration path, and keep implementation docs aligned with code. | Backend unit/integration tests passed with `22 passed, 13 skipped`; Ruff passed. Route-level tests and live migration checks are still pending/in progress in the worktree. | Backend tests/Ruff pass -> route tests cover auth/users/admin/KB/conversation/webhook -> live Alembic upgrade/downgrade passes -> docs/api and PRD status match implemented routes. | `docs/api/endpoints.md`, `docs/development/bug-log.md`, `docs/development/tech-debt-tracker.md` |

## Definition of Done

- [ ] Playbook product routes replace the primary scaffold Example experience.
- [ ] OAuth/OIDC foundation works for Google and Microsoft in configured environments.
- [ ] Athlete signup/profile flow lands directly in chat.
- [ ] Athlete/admin/super-admin role guards are enforced in frontend routes and backend APIs.
- [ ] Playbook database foundation migrations apply and downgrade cleanly against a live local database.
- [ ] Audit logging exists for role changes and foundational admin actions.
- [ ] Chat and admin shells are ready for later feature implementation.
- [ ] Full available backend and frontend test suites pass in the configured project environment.
