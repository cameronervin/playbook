# Phase 2: Athlete AI Experience

## Scope Boundary For This Phase

Phase 2 builds the core athlete-facing AI loop: persisted conversation history,
message submission, streamed assistant responses, grounded KB retrieval,
citations, conversation-scoped file context, and safety/refusal behavior.

Streaming must use an async worker handoff: the message submit API persists the
user turn, creates an assistant placeholder, dispatches a Celery task to execute
the athlete chat agent, and returns a `task_id` plus stream metadata. The Celery
worker runs the LangGraph agent and publishes ordered chunks, progress,
completion, and error events to Valkey Streams, with a pub/sub notification path
for the HTTP stream endpoint. The stream endpoint validates the athlete's
conversation/message ownership and subscribes to the Valkey stream/channel for
the returned `task_id`.

The backend now includes reusable streaming infrastructure under
`backend/app/infrastructure/streaming/` plus `AgentStreamService`. The
message-submit and stream endpoints are wired, and the athlete Celery task now
executes the first Playbook athlete chat agent slice. A live local smoke now
confirms message submit -> Celery worker -> LangGraph execution -> Valkey SSE
progress/chunk/complete events -> assistant persistence for the unsupported
no-source path. Remaining Phase 2 AI work is mostly context engineering,
grounded citation quality, conversation file context, frontend integration, and
eval breadth.

June 28 streaming hardening fixed a backend executor issue where the worker
waited for the graph to finish and then published the entire assistant answer as
one chunk. Validated athlete-facing text is now split into ordered stream chunks
inside the graph after grounding/refusal validation, and the executor only
publishes the terminal completion event after optional first-turn title work.

| Status | Build Area | What To Build | Current Implementation | Validation | Source Specs |
|--------|------------|---------------|------------------------|------------|--------------|
| ☑ | Conversation history APIs | Finish create/list/detail conversation history with messages, citations, and file summaries scoped to the current athlete. | Conversation list/detail routes, service, schemas, and repositories exist for conversations/messages/citations/files. First-send `POST /api/v1/conversations` now creates the conversation with a provisional first-message title, persists the first user message plus assistant placeholder, dispatches the Celery agent task, and returns the conversation detail plus stream metadata. Detail returns bounded messages with citations plus safe top-level file summaries and chunk counts. | `POST /api/v1/conversations` starts first streamed turn with a provisional title -> list returns only athlete-owned rows -> detail returns messages/citations/files -> cross-user access returns 403/404. | `backstage/prd/02-technical-docs/01-playbook/api-specification.md`, `backstage/prd/02-technical-docs/01-playbook/security.md` |
| ☑ | Message submit API | Add `POST /api/v1/conversations/{conversation_id}/messages` to persist follow-up user turns, create assistant placeholders, dispatch Celery agent tasks, and return stream metadata. | First-send and follow-up message routes share the same service orchestration for user message persistence, streaming assistant placeholder creation, `task_id` metadata binding, Celery dispatch, and safe dispatch-failure marking. | POST persists user message -> assistant placeholder has streaming/pending status -> Celery task is enqueued with `task_id` -> response includes `user_message_id`, `assistant_message_id`, `task_id`, `stream_url`, and `status`. | `backstage/prd/02-technical-docs/01-playbook/api-specification.md`, `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | Stream endpoint | Add `GET /api/v1/conversations/{conversation_id}/messages/{message_id}/stream` to stream assistant events for the returned `task_id` by subscribing to Valkey Streams/pub-sub events produced by the Celery worker. | Athlete SSE stream route validates conversation/message ownership plus `task_id` binding; `AgentStreamService` formats durable stream records as SSE and maps LangGraph `messages`, `custom`, and `updates` parts; `ValkeyAgentStreamProvider` continuously reads ordered events until terminal events; the athlete task publishes real progress/chunk/complete/error events. The athlete graph now publishes validated answer text as ordered chunks only after grounding/refusal validation, so no-source and safety-bypass paths stream the safe persisted text instead of raw planner output. A live local smoke confirmed ordered progress events, an answer chunk, a terminal `complete`, and assistant persistence from worker-published Valkey records. | Route/unit tests cover ownership, cursor resume, SSE formatting, Valkey provider behavior with mocks, LangGraph mapping, validation-first answer chunking, safe unsupported streaming, and worker task publishing. Live backend + worker + Valkey smoke confirms submit -> worker publish -> SSE progress/chunk/complete -> terminal close. Remaining hardening: disconnect/unsubscribe smoke and broader operational resiliency. | `backstage/prd/02-technical-docs/01-playbook/api-specification.md`, `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | LangGraph athlete chat graph | Build the athlete chat graph/executor to run inside a Celery worker with conversation ID as thread ID, bounded history, topic/risk labeling, retrieval, generation, persistence, and Valkey stream event publishing. | Athlete-specific state, structured output, load/safety/agent/save nodes, graph topology, and graph-builder compilation now replace the example workflow; `AthleteChatExecutor` delegates to the compiled graph, invokes a separate conversation-title graph on eligible first turns before terminal completion, and handles task failure marking. Live local smoke confirmed the worker executes the compiled graph with LiteLLM, emits lifecycle events, persists the final assistant message, and completes the stream for unsupported answers. Context engineering and answer quality tuning remain under the KB/eval rows. | Worker executes graph async with persisted checkpoint -> bounded history is injected -> topic/risk labels persist -> chunks and lifecycle events publish to Valkey under `task_id` -> first-turn terminal event can include `conversation_title` -> graph can complete or fail safely by conversation/message/task ID. | `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md`, `backstage/prd/02-technical-docs/01-playbook/integration-spec.md` |
| ☑ | KB-grounded answer generation | Retrieve ready visible KB context, generate concise answers, and persist citations from retrieved chunks. | Reusable profile-based KB tool factory exists and athlete chat binds `search_playbook_knowledgebase` for official shared KB plus `search_conversation_files` for ready uploaded conversation files. Both tools register chunks in one source-key registry for structured citation output; KB-service/local provider results carry citation-ready Playbook document ID or conversation file ID, KB-service document ID, stable chunk ID/index, score, and source metadata; `save_state` persists only valid citation metadata to `message_citations` and filters fabricated source keys. Prompt and tool descriptions, not keyword flags, tell the model when policy/process answers need official KB evidence and when to answer unsupported. Remaining validation: context engineering plus golden retrieval/citations against live kb-service data. | NIL/compliance golden question retrieves expected source -> final answer includes citations -> citation document/chunk metadata maps to retrieved result -> zero-hit policy/process prompt declines and persists/streams as unsupported. | `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md`, `backstage/prd/02-technical-docs/02-kb-service/retrieval.md` |
| ☑ | Safety and refusal policy | Implement unsupported, no-source, recruiting, medical/legal/mental-health, harassment/reporting, and emergency handling. | Deterministic athlete safety pre-checks cover emergency, medical, legal, and mental-health guardrails; emergency/sensitive prompts bypass the agent. NIL, compliance, recruiting, harassment/reporting, process grounding, no-source handling, and tool-stop behavior are prompt-led so the agent retains autonomous tool choice. Remaining validation: broader golden safety/eval set. | Emergency prompt refuses advice and shows emergency instructions -> unsupported NIL/compliance prompt declines -> sensitive prompts decline -> safety outcome and answer type persist on message metadata. | `backstage/prd/02-technical-docs/01-playbook/security.md`, `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | Conversation file upload | Add `POST /api/v1/conversations/{conversation_id}/files`, original file storage, extraction, chunking, and conversation-scoped context assembly. | Backend upload, KB-service private ingest, parse/chunk/embed, summary generation, signed status/summary mirroring, private retrieval filters, backend conversation-file agent tool/citation wiring, storage/ingest failure hardening, redaction checks, retry/watchdog coverage, local private-file smoke support, and frontend direct-upload UI are implemented. The frontend can attach files to existing conversations or queue files on the home/new-chat screen until the first message creates a conversation. Conversation files mirror KB-service document ID, internal summary, status, and chunk count while safe athlete responses expose status/count only; the agent prepares trusted ready-file scope, injects a bounded file-summary manifest for orientation, and calls `search_conversation_files` when uploaded-file evidence is needed. Remaining: live grounded citation smoke against seeded local data. | Upload stores original -> extraction status moves uploaded/extracting/ready/failed -> chunks are available only to owning conversation -> shared KB search excludes athlete uploads -> athlete answers can cite ready uploaded files. | `backstage/prd/02-technical-docs/01-playbook/data-model.md`, `backstage/prd/02-technical-docs/01-playbook/integration-spec.md` |
| ☑ | Athlete chat frontend | Build authenticated chat UI with conversation sidebar, message composer, streaming transcript, citation display, upload control, loading/error states, and responsive layout. | Auth/profile/chat routes and the chat workspace shell exist with conversation history/detail loading states, first-send and follow-up submit wiring, SSE transcript streaming through the returned stream URL, terminal title updates from `conversation_title`, citation/source panel behavior, recoverable stream error state, direct conversation-file upload progress/status for existing conversations, and sticky near-bottom auto-scroll that pauses when the athlete scrolls up and resumes via near-bottom detection or a jump-to-latest control. Remaining frontend work is broader live citation validation against seeded KB data. | Athlete opens chat -> can create/select conversation -> sends prompt and sees streamed response -> first message shows a provisional title and first-turn completion can replace it -> citations render below answer -> upload control reflects file status -> responsive layout remains usable. | `backstage/prd/01-user-stories/epic-2-athlete-ai-chat-experience.md`, `.claude/rules/13-frontend-design-standards.md`, `.claude/style/ui-patterns.md` |
| ☑ | Tests and eval smoke | Add service/API/frontend tests plus early golden smoke checks for grounded answers and refusals. | Backend repository, route, stream-service, worker, graph-builder, executor, conversation-file hardening, mixed citation, KB-service retry/watchdog, redaction, and smoke-helper tests exist; a live local smoke now proves submit -> worker -> graph -> SSE -> persistence for an unsupported answer. Frontend chat tests now cover streaming content display, pending thinking state, source-panel behavior, file uploads, and near-bottom auto-scroll pause/resume/jump behavior. Live grounded KB citation golden cases and broader eval sets remain. | Route tests cover ownership and stream behavior -> live smoke covers the asynchronous agent streaming loop -> frontend chat tests pass -> retrieval/refusal smoke cases pass against seeded/fake providers. | `backstage/prd/02-technical-docs/01-playbook/eval-framework.md`, `.claude/rules/06-testing.md` |

## Definition of Done

- [x] Athletes can create, revisit, and continue conversations with provisional and AI-generated first-turn titles.
- [x] User messages submit through the API, enqueue a Celery agent task, return `task_id`, and assistant responses stream from Valkey Streams/pub-sub before persisting correctly.
- [x] Grounded answers include citations tied to retrieved KB chunks.
- [x] Unsupported and sensitive prompts decline safely.
- [x] Conversation file uploads work without adding files to the shared KB.
- [x] Athlete chat UI handles loading, streaming, citations, errors, and responsive layouts.
- [x] Phase 2 tests and relevant eval smoke checks pass.
