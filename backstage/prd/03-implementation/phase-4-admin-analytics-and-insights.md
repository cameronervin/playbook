# Phase 4: Admin Analytics and Insights

## Scope Boundary For This Phase

Phase 4 builds the admin analytics dashboard and insight agents for recent
athlete queries. It does not create document-edit automation, report exports,
or integrations that act in external athletic systems.

Admin conversational agent responses must use the same async stream bridge as
athlete chat: the admin question API persists the admin turn, creates an
assistant placeholder, dispatches a Celery task, and returns `task_id` plus
stream metadata. The worker executes the admin chat agent and publishes ordered
chunks, progress, completion, and error events to Valkey Streams, with pub/sub
notification for the HTTP stream endpoint. Long-running non-conversational jobs
in this phase, such as dashboard insight runs, remain polled by `run_id`.

| Status | Build Area | What To Build | Current Implementation | Validation | Source Specs |
|--------|------------|---------------|------------------------|------------|--------------|
| ☑ | Analytics and admin chat data model | Use existing conversation/message metadata plus dashboard insight and admin chat tables as the durable foundation. | ORM models and migrations exist for `dashboard_insight_runs`, `dashboard_insights`, `admin_chat_sessions`, and `admin_chat_messages`; dashboard insight and admin chat repositories are implemented. | Migration includes analytics/admin chat tables -> repositories can create/list/update runs, outputs, sessions, and messages -> seeded data can be queried by organization/window. | `backstage/prd/02-technical-docs/01-playbook/data-model.md` |
| ☑ | Analytics repositories/services | Aggregate query volume, topics, unanswered/declined questions, and NIL/compliance/recruiting risk from conversation messages. | `AdminAnalyticsRepository` and `AdminAnalyticsService` aggregate org/window-scoped query records, SQL-filtered review rows, uncapped summaries, anonymized owner keys, and bounded snapshots for dashboard insight generation. | Seeded messages produce expected volume/topic/risk/unanswered counts -> filters by date/topic/risk work before pagination -> summaries are not capped by query-review row limits -> services never expose athlete owner identity by default. | `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md`, `backstage/prd/02-technical-docs/01-playbook/security.md` |
| ☑ | Analytics APIs | Implement admin-only summary and anonymized query review endpoints. | `/api/v1/admin/analytics/summary` and `/api/v1/admin/analytics/queries` are implemented with admin role guards, anonymized responses, repeatable plural `topic_labels`/`risk_labels` filters, and strict query-param validation. | `GET /admin/analytics/summary` returns volume/topics/unanswered/risk counts -> `GET /admin/analytics/queries` returns query text with anonymous user key -> athlete name/email omitted -> unsupported query params return 422. | `backstage/prd/02-technical-docs/01-playbook/api-specification.md`, `backstage/prd/01-user-stories/epic-4-admin-analytics-and-insights.md` |
| ◐ | Admin dashboard frontend | Build dashboard views for summary cards, topic/risk breakdowns, unanswered query review, filters, and insight status. | `/admin` Insights now reads real analytics summary/query rows and current dashboard insight output through TanStack Query, renders loading/empty/current/processing/failed states, and posts manual 7d/30d runs with concrete UTC windows before polling by `run_id`. Custom date-range picking remains out of MVP scope until a picker is added. | Admin dashboard loads summary -> query list anonymizes owners -> filters update data -> empty/loading/error states render cleanly -> athlete cannot access admin pages. | `backstage/prd/01-user-stories/epic-4-admin-analytics-and-insights.md`, `.claude/rules/13-frontend-design-standards.md` |
| ☑ | Dashboard insight generation | Implement manual and scheduled insight runs that summarize recent chat/analytics data into durable dashboard outputs. | Manual run APIs, run/output APIs, `DashboardInsightService`, dashboard insight LangGraph chain/graph/nodes/executor/tools, Celery task, nightly beat dispatch, frontend current/run polling, overlapping `/current` window matching, and the deterministic `dashboard_insights` eval golden set are implemented. | Nightly job creates run -> manual POST returns `run_id` -> run status transitions pending/processing/completed/failed -> completed output persists cards, summary, topic/risk output, and attention areas -> `/current` returns the latest completed overlapping run output -> eval checks topic/risk labels, source grounding, and no fabricated metrics. | `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md`, `backstage/prd/02-technical-docs/01-playbook/integration-spec.md` |
| ☑ | Admin chat side panel | Add admin chat sessions/messages over authorized analytics and dashboard insight data, with Celery-executed agent responses streamed through Valkey Streams/pub-sub by `task_id`. | Admin chat ORM models, session/message repositories, service, route surface, dispatcher payload, stream validation, audit actions, LangGraph prompt/middleware/chain/state/graph/nodes/executor/tools, worker task, provider cache, and frontend API/SSE side-panel wiring are implemented. | Admin creates/loads own session -> sends analytics question -> API persists question and assistant placeholder -> Celery task is enqueued with `task_id` -> stream endpoint validates admin/session/message ownership and subscribes to the Valkey stream/channel for `task_id` -> answer references authorized analytics/insight records -> unsupported questions decline -> session history persists. | `backstage/prd/02-technical-docs/01-playbook/api-specification.md`, `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | Privacy, audit, and observability | Enforce anonymization, admin authorization, audit manual insight triggers, and trace analytics/insight/admin-chat events without unnecessary PII. | Analytics routes are admin-only, athlete routes return 403, snapshots use stable HMAC anonymous owner keys, dashboard/admin chat tools read only anonymized runtime context, manual insight triggers and admin chat session/question submissions write audit events, and logs use IDs/error types. Admin chat worker re-authorizes admin/org/session/message/task binding and deterministically refuses identity/action requests. Runtime Langfuse tracing is wired for app and backend worker graph runs with allowlisted trace metadata and a recursive mask for prompts, messages, source URIs, storage keys, signed URLs, emails, tokens, and secrets. | Athlete identity omitted from analytics -> manual insight run writes audit event -> admin chat logs/traces include request/run/session IDs without athlete owner identity -> route guards return 403 for athletes -> Langfuse startup readiness and trace masking are covered by focused tests. | `backstage/prd/02-technical-docs/01-playbook/security.md`, `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ◐ | Tests and docs | Add backend service/API tests, frontend dashboard tests, insight job tests, admin chat stream tests, and docs updates. | Backend tests cover analytics route anonymization, manual run dispatch/audit/status/current/output/failure visibility, dashboard insight chain/tools/state, admin chat route/stream contract, worker dispatcher/tasks, graph provider, builders, and tool registry. Frontend tests cover admin shell chat API/SSE wiring plus real dashboard summary/current/run fetches, manual generation, empty-current, failed-run, and athlete-denial states. The `dashboard_insights` eval structural golden set is registered; broader release eval gates remain Phase 5. | Analytics service tests pass -> route tests cover auth/anonymization, polled insight runs, and admin chat stream ownership/task binding -> frontend tests pass -> insight/admin-chat eval smoke cases exist -> API docs list implemented routes. | `backstage/prd/02-technical-docs/01-playbook/eval-framework.md`, `backstage/api/endpoints.md` |

## Definition of Done

- [x] Admin dashboard shows query volume, topics, unanswered questions, and NIL/compliance/recruiting risk.
- [x] Athlete identity is anonymized in backend analytics payloads.
- [x] Nightly and manual dashboard insight runs work through polled `run_id` status on the backend.
- [x] Admin chat side panel answers analytics questions from authorized data, streams agent output by `task_id`, and persists admin session history.
- [x] Manual insight triggers are audited; admin chat submissions and privacy-safe runtime trace behavior are covered.
- [x] No export/report feature is included in MVP.
