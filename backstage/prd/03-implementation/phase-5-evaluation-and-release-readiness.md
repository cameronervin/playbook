# Phase 5: Evaluation and Release Readiness

## Scope Boundary For This Phase

Phase 5 creates the quality, safety, security, abuse-prevention, observability,
and affiliation gates needed before an athlete-facing prototype is considered
ready. It should package release validation into documented commands that future
contributors can run locally and in CI.

| Status | Build Area | What To Build | Current Implementation | Validation | Source Specs |
|--------|------------|---------------|------------------------|------------|--------------|
| ☑ | Eval harness foundation | Keep the backend eval package, dataset/rubric locations, CLI shape, and result storage ready for real Playbook cases. | `backend/evals/` now has offline dataset validation, strict release CLI behavior, result metadata, deterministic judges, production-executor spec scaffolds, Langfuse v4 experiment-runner compatibility, target thresholds for every active release rubric, and Langfuse/Ragas integration. | `validate-datasets` passes locally -> eval unit tests pass -> failed thresholds or isolated judge errors exit non-zero in strict runs -> generated result JSON/Markdown stays out of git. | `backstage/prd/02-technical-docs/01-playbook/eval-framework.md` |
| ☑ | Golden datasets | Create golden cases for NIL, compliance, internal process, unknown/no-source, conflict/newer-source, emergency, admin analytics, and admin chat scenarios. | Curated `athlete_chat`, `admin_chat`, expanded `dashboard_insights`, `conversation_title`, and shared KB fixture YAMLs exist with realistic syntax, typos, rushed language, and adversarial examples. | Golden dataset files exist -> each case maps expected source/behavior/refusal -> schema validation fails on malformed cases -> dataset coverage maps to user stories. | `backstage/prd/02-technical-docs/01-playbook/eval-framework.md`, `backstage/prd/01-user-stories/epic-5-safety-governance-and-release-readiness.md` |
| ☑ | Retrieval and citation evals | Verify expected docs are retrieved and cited, and fabricated citations fail. | Deterministic retrieval hit, citation integrity, source freshness, and privacy leakage judges are implemented; athlete chat uses Ragas retrieval/generation rubrics for KB cases with target thresholds for context precision/recall, faithfulness, and answer relevancy. | Expected source appears in top-K -> cited source was retrieved -> no fabricated citation passes -> failed docs are excluded from retrieval evals -> strict run gates freshness and Ragas means. | `backstage/prd/02-technical-docs/01-playbook/eval-framework.md`, `backstage/prd/02-technical-docs/02-kb-service/retrieval.md` |
| ☑ | Answer/refusal/emergency evals | Verify answer quality, no-source refusal, sensitive-topic refusal, off-topic refusal, and emergency instructions. | Deterministic answer/refusal/emergency behavior checks and LLM qualitative rubrics are wired for athlete chat, admin chat, and conversation titles; title and KB/admin prompts now emphasize canonical eval labels, newest-source citation, and allowed-reference discipline. | NIL/compliance answers decline without KB support -> emergency prompts refuse advice and show emergency instructions -> medical/legal/mental-health prompts decline -> non-athletics prompts decline and steer back to athletics -> refusal labels persist -> qualitative rubric floors gate strict runs. | `backstage/prd/02-technical-docs/01-playbook/security.md`, `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | Admin analytics and chat evals | Validate dashboard insight summaries and admin chat answers against seeded analytics data. | The dashboard-insights deterministic suite is expanded to 12 cases; admin chat has a 32-case golden set plus behavior/reference/privacy deterministic gates. | Seeded analytics produce expected dashboard insight themes -> admin chat references authorized analytics/insight records -> admin chat declines out-of-scope questions. | `backstage/prd/02-technical-docs/01-playbook/eval-framework.md`, `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md` |
| ☑ | Security and audit release checks | Validate route guards, API auth, analytics anonymization, audit immutability, and service-to-service auth. | `deploy/scripts/release-validate.sh` now packages targeted backend auth/admin/audit/anonymization checks plus KB-service API contract/redaction checks into the release gate; `backend/tests/integration/test_release_authz_gates.py` explicitly covers unauthenticated workspace rejection, athlete admin-API rejection, and admin super-admin mutation rejection. | Athlete admin APIs return 403 -> super-admin-only routes enforce role -> admin analytics omits names/emails -> audit mutation route unavailable -> KB service rejects missing/invalid service tokens -> `./deploy/scripts/release-validate.sh` passed locally on 2026-07-02. | `backstage/prd/02-technical-docs/01-playbook/security.md` |
| ☑ | Rate limiting and abuse-prevention checks | Validate production-facing controls for request bursts, brute-force attempts, chat/LLM cost abuse, uploads, admin-triggered jobs, and large response/page requests. | Backend app-level sliding-window limits cover auth, athlete chat, admin chat, manual insight runs, uploads, and list/query endpoints; production env examples enable shared Valkey storage; deterministic LiteLLM gateway checks validate alias-only app config, no wildcard proxy model, no direct provider keys in app env examples, and non-secret virtual-key budget/RPM policy shape. Edge/WAF controls remain deployment infrastructure. | Auth/login callback abuse returns 429 or equivalent edge block -> athlete chat/admin chat/manual insight runs are limited per user, organization, and IP/window -> upload presign/complete and large file requests respect size/body/time limits -> API list endpoints keep max page caps -> LiteLLM virtual keys have per-environment budget/rate limits -> limit events are logged with request/org/user IDs but no prompts, files, tokens, raw IP addresses, or secrets. | `backstage/prd/02-technical-docs/01-playbook/security.md`, `backstage/prd/02-technical-docs/01-playbook/integration-spec.md`, `backstage/guides/deployment.md` |
| ☑ | Observability and affiliation checks | Verify useful logs/traces and scan product copy for explicit university affiliation claims. | `evals.cli release-checks` validates request/log redaction/tracing anchors and scans runtime copy/code for protected university affiliation claims without Langfuse credentials; backend and KB-service redaction tests cover prompts, source text, signed URLs, tokens, raw IP fields, athlete name/email, and provider subject, while Langfuse trace tests preserve eval-critical content and mask sensitive fragments. | Logs include request IDs and product IDs without tokens/secrets/prompts/source text/raw IP/athlete identity -> Langfuse traces retain prompt/message/source content for evaluation while masking secrets/source locations/identity fields -> chat/retrieval/ingestion/insight events are traceable -> copy scan finds no protected affiliation claims -> deterministic release checks returned `PASS release checks (0 issue(s))` on 2026-07-02. | `backstage/prd/02-technical-docs/01-playbook/agentic-framework.md`, `backstage/prd/02-technical-docs/01-playbook/security.md` |
| ☑ | Release validation command and docs | Document one local/CI release command or checklist for tests, evals, security checks, and smoke tests. | `./deploy/scripts/release-validate.sh` runs targeted backend, eval, release-check, KB-service, and frontend gates by default, with `--live-evals` for strict Langfuse-backed evals when services and credentials are available; its deploy shell syntax step now validates every deploy script before the test gates. Setup, deployment, eval, API, and eval-framework docs now reference the command and 429/LiteLLM policy posture. | Documented command/checklist runs backend, frontend, KB-service, eval, security, observability, and copy checks -> failures point to actionable logs/results -> local run passed on 2026-07-02. | `backstage/guides/setup.md`, `backstage/guides/deployment.md`, `backstage/development/bug-log.md`, `backstage/development/tech-debt-tracker.md` |
| ☑ | AppSec scanner CI and SBOM artifacts | Add OSS-only blocking scanner gates for SAST, SCA, secrets, Trivy filesystem/config/image scans, release SBOM artifacts, and staging DAST. | `.github/workflows/security-scan.yml` runs on PRs, `main`, schedule, and manual dispatch; `.github/workflows/release-readiness.yml` packages deterministic release validation plus scanner/SBOM jobs for PRs, `main`, release tags, and manual dispatch; `.github/workflows/dast.yml` runs OWASP ZAP baseline scans against approved staging URLs; `deploy/scripts/security-scan.sh` and `deploy/scripts/generate-sbom.sh` provide local/CI entrypoints with artifacts under `.artifacts/`. | Semgrep CE/Bandit fail on high-signal SAST findings -> pip-audit/OSV/npm audit fail vulnerable dependency gates -> Gitleaks scans full history with redaction -> Trivy scans source, deploy config, and production images -> CycloneDX JSON SBOMs upload as release artifacts -> ZAP reports upload as `dast-artifacts`. | `backstage/guides/security_scanning.md`, `backstage/security/evidence/README.md`, `backstage/prd/03-implementation/_production-readiness-playbook.md` |
| ◐ | Staging load-testing gate | Add repeatable Locust checks for cheap API capacity by default and opt-in live agent/SSE paths when budget is approved. | `load-tests/` contains Locust scenarios, threshold hooks, unit tests, and sanitized artifact manifests; `deploy/scripts/load-test.sh` provides smoke/baseline/live-agent/rate-limit profiles; `release-validate.sh --staging-load` and `.github/workflows/release-readiness.yml` manual `staging_load` wire the staging gate and artifact upload. | `cd load-tests && uv run pytest -q` passes -> `uv run ruff check .` passes -> shell syntax passes -> dry run prints a sanitized command -> staging run uploads `load-test-artifacts` before production. Keep P0 load-testing gate open until real staging artifacts are attached. | `backstage/prd/03-implementation/_production-readiness-playbook.md`, `backstage/guides/deployment.md`, `backstage/guides/setup.md` |
| ◐ | Deployment hardening and recovery evidence hooks | Harden the Compose/nginx deployment boundary and provide operator commands for deploy readiness, migration evidence, encrypted DB backup, and restore drills. | Production nginx redirects HTTP to HTTPS, applies security headers/request limits/proxy timeouts, and logs privacy-safe request metadata; app runtime images run as non-root where feasible; prod env template includes public URL, OAuth state, cookie-domain, and direct-LLM break-glass defaults; `backup-db.sh`, `restore-db.sh`, `validate-nginx.sh`, and `readiness-evidence.sh` provide deploy/recovery evidence hooks. | `validate-nginx.sh` passed locally on 2026-07-02 with a throwaway cert -> `readiness-evidence.sh prod` should record compose health plus backend/KB Alembic current/head state -> encrypted backup and restore-drill scripts produce checksum and target-DB evidence. Keep P0 deployment/recovery gates open: `docker compose -f deploy/compose/base.yml -f deploy/compose/prod.yml config --quiet` and `readiness-evidence.sh prod` are blocked locally until out-of-band `deploy/envs/.env.prod` secrets are supplied. | `backstage/guides/deployment.md`, `backstage/prd/03-implementation/_production-readiness-playbook.md` |
| ◐ | Sentry production monitoring | Add privacy-safe hosted Sentry monitoring for frontend, backend API/workers, and KB API/workers. | Next.js uses `@sentry/nextjs` with App Router instrumentation and optional source-map upload; backend and KB-service use `sentry-sdk` FastAPI/Starlette/Celery integrations. Runtime config is disabled by default and requires `SENTRY_ENABLED=true` plus a DSN. Event scrubbers remove request bodies, cookies/auth headers, prompts, source text, signed URLs, tokens, raw IP fields, local variables, and Sentry user identity. Session Replay, user feedback, and Sentry Logs remain disabled. | Unit tests cover disabled init paths, privacy options, scrubbers, backend 500 capture, and KB worker idempotent init. Keep monitoring gates open until hosted projects, alert routing, source-map upload evidence, and staging/prod smoke events are attached. | `backstage/guides/deployment.md`, `frontend/FRONTEND.md`, `backstage/prd/03-implementation/_production-readiness-playbook.md` |
| ◐ | P1 institutional rollout docs | Create production operational docs for risk-based coverage, incident response, data lifecycle, and external review scheduling. | `backstage/production/` now contains the rollout docs and `coverage-policy.yaml`; `evals.cli release-checks` validates that every required P1 risk surface has committed test evidence. Coverage policy and incident runbook gates have evidence. Data lifecycle and external review remain open until approval/scheduling placeholders are filled. | `cd backend && uv run pytest tests/unit/evals/test_release_checks.py -q` passed (`13 passed`) -> `cd backend && uv run --group evals python -m evals.cli release-checks` returned `PASS release checks (0 issue(s))` -> `./deploy/scripts/release-validate.sh` passed on 2026-07-02. | `backstage/production/README.md`, `backstage/prd/03-implementation/_production-readiness-playbook.md` |

## Latest Eval Calibration Evidence

- 2026-07-02: eval target thresholds are configured for every active release
  rubric/spec, and generated result JSON/Markdown files are gitignored while
  preserving `backend/evals/results/README.md` and `.gitkeep`.
- 2026-07-02: `cd backend && uv run --group evals pytest tests/unit/evals -q`
  passed (`55 passed`), `validate-datasets` returned `datasets valid`, and
  `release-checks` returned `PASS release checks (0 issue(s))`.
- 2026-07-02: focused live Langfuse v4 runs passed for
  `dashboard_insights-2026-07-02T18:18:24+00:00` and
  `conversation_title-2026-07-02T18:19:29+00:00`. The full strict live suite
  remains open because repeated local `run-all --strict --max-concurrency 5`
  attempts stalled during `athlete_chat` external DNS resolution before a result
  artifact was written.
- 2026-07-02: P1 coverage-manifest release checks passed:
  `cd backend && uv run pytest tests/unit/evals/test_release_checks.py -q`
  (`13 passed`) and `cd backend && uv run --group evals python -m evals.cli release-checks`
  (`PASS release checks (0 issue(s))`). The full local
  `./deploy/scripts/release-validate.sh` wrapper also passed after the P1 docs
  and manifest were added.

## Definition of Done

- [x] Golden evals exist and run with a documented command.
- [x] Retrieval, citation, answer, refusal, emergency, admin insight, and admin chat eval checks are implemented.
- [x] Security and audit validation passes.
- [x] Rate limiting, upload/body limits, LLM budget limits, and abuse telemetry validation passes.
- [x] Observability validation confirms no secrets/tokens are logged.
- [x] Affiliation-copy check passes.
- [x] Full release validation command or checklist is documented.
- [x] OSS scanner, SBOM artifact, and DAST workflows are documented.
- [ ] Staging Locust load-test artifacts are attached before production traffic.
