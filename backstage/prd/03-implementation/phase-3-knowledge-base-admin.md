# Phase 3: Knowledge Base Admin

## Scope Boundary For This Phase

Phase 3 turns the KB service and backend document control plane into an
admin-manageable Playbook knowledge base for athlete answers. Some backend
control-plane work started in Phase 1; this phase completes the admin UX,
contract alignment, status/retry flows, and end-to-end ingestion/search
verification.

| Status | Build Area | What To Build | Current Implementation | Validation | Source Specs |
|--------|------------|---------------|------------------------|------------|--------------|
| ☑ | Backend KB document APIs | Keep/admin-harden list, upload, get, metadata patch, retry, delete, and signed webhook APIs. | Backend routes/services exist for all listed operations. Admin upload uses JSON direct-upload intent/completion routes with `metadata_tags` support; route-level tests now cover storage verification, duplicate completion, audit events, outbox enqueue, reserved metadata key rejection on patch, explicit `source_date: null` clearing, omitted `source_date` preservation, and linked retry/delete calls into KB-service. Live validation passed with backend, backend Celery, KB API, and KB workers run via `uv` against local Postgres, Valkey, MinIO, and LiteLLM. | Admin APIs enforce role guards -> upload intent creates document and audit event -> browser-style MinIO POST succeeds -> completion verifies storage and queues one ingest outbox row -> worker dispatches to KB-service -> webhook links the KB document and mirrors processing -> metadata/retry/delete write audit events -> delete removes backend row, KB-service document, vectors, and original file -> athlete access returns 403. Automated backend focused suite passed `47` tests; live smoke passed on 2026-06-29 with final evidence: `backend_doc_count=0`, `kb_doc_count=0`, `embedding_count=0`, audit actions include upload/uploaded/metadata/retry/delete. | `backstage/prd/02-technical-docs/01-playbook/api-specification.md`, `backstage/prd/02-technical-docs/01-playbook/security.md` |
| ☑ | KB-service ingestion/retrieval integration | Verify backend `LocalKBProvider` against canonical kb-service routes for configuration, ingest, status, search, retry, and delete. | Provider calls `/api/kb/configuration/resolve`, `/api/kb/ingest/document`, `/api/kb/status/documents/{document_id}`, `/api/kb/search`, `/api/kb/documents/{document_id}/retry`, and `/api/kb/documents/{document_id}`. Added `backend/scripts/phase3_kb_e2e_smoke.py` to prove admin direct upload -> backend outbox -> kb-service ingest -> webhook mirror -> provider search -> athlete chat stream -> persisted citation -> failed document exclusion -> retry -> delete cleanup. Live validation ran backend, backend Celery, KB API, and KB workers via `uv` against local Postgres/pgvector, Valkey, MinIO, and LiteLLM aliases for embeddings, summaries, and chat. | 2026-06-29 live pass: KB-service smoke with conversation-file isolation passed in semantic mode; Phase 3 smoke passed with ready admin document searchable by `LocalKBProvider`, athlete SSE stream completed with persisted KB citation, malformed DOCX failed and did not appear in retrieval, retry reprocessed the linked document to ready, and delete removed backend row plus searchable KB result. Focused verification passed backend `64` tests + Ruff, kb-service `53` tests + Ruff, and frontend KB/admin `28` tests + typecheck + lint. | `backstage/prd/02-technical-docs/02-kb-service/api-contracts.md`, `backstage/prd/02-technical-docs/02-kb-service/ingestion-pipeline.md`, `backstage/prd/02-technical-docs/02-kb-service/retrieval.md` |
| ☑ | API contract reconciliation | Reconcile PRD KB-service API docs with implemented route shapes before building more admin UX. | Hard cutover complete: kb-service exposes the PRD semantic routes only, backend `LocalKBProvider` uses those routes, old scaffold routes are absent from OpenAPI and return 404, and route/provider tests cover the canonical contract. | PRD API contracts match implemented route shapes -> backend provider and docs agree -> no phase task points engineers at stale endpoints. | `backstage/prd/02-technical-docs/02-kb-service/api-contracts.md`, `backstage/api/endpoints.md` |
| ◐ | Admin document UI | Build document list, upload dialog/form, status display, failure details, metadata edit, retry, and delete/archive controls. | Admin route and collection-first KB UI exist with direct-upload intent/completion flow, local upload progress, recoverable local upload failure retry, document status labels, metadata drawer, retry, and delete/archive controls. The browser contract now exposes tags/source date only; `is_official` and `priority` remain backend-internal MVP defaults. Remaining validation includes live MinIO/S3-compatible admin upload e2e and broader failure UX smoke. | Admin opens document page -> uploads supported file -> sees uploaded/processing/ready/failed -> edits metadata -> retries failed doc -> deletes/archive doc -> UI handles empty/error/loading states. Focused KB/admin frontend suite passed `30` tests and full frontend suite passed `128` tests. | `backstage/prd/01-user-stories/epic-3-knowledge-base-and-document-operations.md`, `.claude/rules/13-frontend-design-standards.md` |
| ◐ | Metadata and ranking fields | Support tags, source date, and visibility metadata across backend, KB service, search results, and UI. | Backend public API supports tags/source date and default visibility. All admin-uploaded shared KB documents are official by definition for MVP, and priority is normalized internally but not exposed or used for ranking. | Upload and patch persist metadata tags/source date -> search result returns source freshness/tags -> citations can display source metadata -> future visibility policy remains all-athletes by default. | `backstage/prd/02-technical-docs/01-playbook/data-model.md`, `backstage/prd/02-technical-docs/02-kb-service/data-model.md`, `backstage/prd/02-technical-docs/02-kb-service/retrieval.md` |
| ◐ | Status, retry, and failure UX | Show ingestion lifecycle and let admins recover failed parsing/indexing without exposing broken docs to athlete retrieval. | Backend retry service, webhook status mapping, and admin UI status/failure controls exist for upload-pending, queued, processing, ready, and failed documents. Live smoke fixed and covered webhook stage-success mapping, non-regression from ready back to processing on late stage webhooks, and kb-service retry reset of stale successful ingestion logs. Remaining validation is broader browser failure UX. | Failed doc shows reason -> retry restarts ingestion -> no-text/corrupt file remains failed with reason -> failed docs are excluded from retrieval -> retry/re-upload audits are written. | `backstage/prd/01-user-stories/epic-3-knowledge-base-and-document-operations.md`, `backstage/prd/02-technical-docs/02-kb-service/ingestion-pipeline.md` |
| ☑ | KB-service operations hardening | Verify parser/chunker/embed/vectorstore workers, service auth, S3 staging cleanup, and document deletion behavior under local dev conditions. | KB service has service auth, configuration, ingest, status, search, document delete routes, worker tasks, parser routing, chunking, embedding, pgvector helpers, and canonical source summaries. Operations hardening now preserves Uvicorn access-log formatter args under redaction, raises noisy SDK loggers above DEBUG even when local `LOG_LEVEL=DEBUG`, keeps health unauthenticated while bad bearer tokens are rejected, deletes original/page/chunk staging objects plus embeddings on document delete, and makes stale `load_vector_task` invocations no-op for deleted documents before smart-defer or reissue logic. The KB smoke script also asserts post-delete status/search/S3/staging cleanup and adds optional `--check-litellm-summary` validation for the `playbook-fast` LiteLLM alias. | KB service tests pass -> service auth rejects missing/bad token -> staged files are cleaned up after successful load and delete -> delete removes document rows/embeddings/S3 original as intended -> stale retry/finalizer tasks cannot recreate orphan embeddings after delete -> summary fallback remains runtime resilience while smoke/preflight can detect LiteLLM alias auth/config failures. Focused hardening suite passed `40` tests; full KB-service suite passed `212` tests plus Ruff; backend provider/log smoke helper suite passed `23` tests with `DEBUG=true`; live `uv` KB smoke passed with `--include-conversation-file --check-litellm-summary` and `cleanup=deleted`; live Phase 3 backend-to-KB-to-athlete smoke passed. | `backstage/prd/02-technical-docs/02-kb-service/architecture.md`, `backstage/prd/02-technical-docs/02-kb-service/operations-security.md` |
| ◐ | Tests and docs | Add integration/route/frontend tests and update docs for the final KB admin/API behavior. | Main API docs, direct-upload architecture docs, setup/MinIO docs, and PRD upload contracts describe implemented direct-upload behavior. Current verification passed backend focused (`64`), KB-service full suite (`212`), backend provider/log smoke helper suite (`23` with `DEBUG=true`), backend Ruff, KB-service Ruff, focused frontend KB/admin (`28`), frontend typecheck, frontend lint, KB-service live smoke with private source + summary preflight coverage, and the Phase 3 backend-to-KB-to-athlete smoke via `uv`-run app servers. Remaining docs/test closure tracks the rest of the admin UI rows. | Backend route tests pass -> frontend admin document tests pass -> KB e2e smoke documented -> API docs and PRD contracts match implementation. | `backstage/api/endpoints.md`, `backstage/guides/setup.md`, `backstage/prd/02-technical-docs/02-kb-service/api-contracts.md` |

## Definition of Done

- [x] Admins can upload, view, tag, retry, and delete/archive KB documents.
- [x] Ready documents are searchable by the athlete chat agent.
- [x] Failed documents do not influence answers.
- [x] Metadata needed for citations and conflict handling is returned by retrieval.
- [x] Document admin actions are audited.
- [x] KB-service API contract docs match the route shapes used by the backend provider.
