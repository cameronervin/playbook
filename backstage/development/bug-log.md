# Bug Log

> Track bugs found during development and their resolution. Add a row when a bug
> is discovered; update `Status` and `Fix` as it progresses.

## Severity Scale

| Severity | Meaning |
|----------|---------|
| Critical | Data loss, security issue, or production outage |
| High | Major feature broken, no workaround |
| Medium | Feature impaired, workaround exists |
| Low | Cosmetic or minor inconvenience |

## Status Values

`Open` · `In Progress` · `Fixed` · `Won't Fix` · `Closed`

## Bugs

| ID | Date | Severity | Description | Status | Fix |
|----|------|----------|-------------|--------|-----|
| B-001 | 2026-06-07 | Low | Login page border utilities did not compile to the intended warm hairline tokens, causing visual drift from the Claude design reference. | Fixed | Added missing Tailwind v4 border theme tokens and kept login visual regression tests focused on SSO-only behavior. |
| B-002 | 2026-06-08 | Low | First-time athlete profile completion drifted from the login/auth design shell, using a generic page stage and card instead of the shared horizon background and raised auth card. | Fixed | Added shared auth stage/card components, moved `/login` and `/profile` onto them, and covered the profile shell plus SSO tile alignment with frontend tests. |
| B-003 | 2026-06-08 | Low | First-time athlete profile completion still used a wider, left-aligned, oversized modal hierarchy that did not match the compact login auth modal. | Fixed | Removed the profile-only wide card variant and normalized the profile lockup, heading, copy, fields, and CTA to the compact auth-card rhythm. |
| B-004 | 2026-06-08 | Low | Login and profile could drift again because each feature screen owned the auth background stage instead of sharing it at the route-layout boundary. | Fixed | Grouped `/login` and `/profile` under `src/app/(auth)/` and moved the shared `AuthStage` into the route-group layout. |
| B-005 | 2026-06-08 | Medium | Admin Insights rendered a rushed placeholder dashboard instead of the Claude design: standalone KPI cards, no AI-summary hierarchy, missing topic/risk/chart modules, and no right-side analytics chat panel. | Fixed | Added a shared workspace shell, grouped `/chat` and `/admin` under `src/app/(workspace)/`, rebuilt Admin Insights from typed fixtures, and added tests for the target dashboard and analytics chat panel. |
| B-006 | 2026-06-08 | Low | Admin workspace typography and header controls rendered larger than the Claude wireframe because compact product type roles were not exposed globally and small buttons used Tailwind default sizing. | Fixed | Added shared compact typography utilities, made `Button size="sm"` the canonical 36px app control, and updated admin pages to consume the shared hierarchy. |
| B-007 | 2026-06-08 | Low | Admin Insights header controls still looked mismatched because the time-window filter and adjacent action buttons used different class paths and no shared width. | Fixed | Reused the compact `pb-ui-sm` control typography in `Button size="sm"` and applied a shared 156px header-control width to the filter, regenerate, and explore actions. |
| B-008 | 2026-06-08 | Low | Super-admin Users & roles used a rushed card/select scaffold and a different page header structure than Admin Insights, drifting from the Claude design reference. | Fixed | Added a shared admin page scaffold, moved Insights/KB/Users onto it, and rebuilt Users & roles with search, table rows, role pills, locked current-user state, and Radix role menus. |
| B-009 | 2026-06-08 | Low | Admin header controls had too much side padding and the admin rail labels read larger than the rest of the compact dashboard UI. | Fixed | Added global admin header-control and nav-item utilities, tightened the header control width/padding, and moved admin rail label/icon sizing onto shared globals. |
| B-010 | 2026-06-08 | Low | Users & roles row actions and role menus used one-off sizing that overpowered table text, and search floated in a detached toolbar instead of belonging to the table surface. | Fixed | Added compact admin table/menu globals, moved Users search into the table header band, and updated tests/docs to prevent generic oversized table controls. |
| B-011 | 2026-06-08 | Low | Admin account menu showed both Chat workspace and a self-linking Admin dashboard item, instead of only offering the opposite workspace. | Fixed | Removed the admin-menu self-link, kept Chat workspace as the only admin workspace switcher, and added role-based account-menu tests for chat/admin. |
| B-012 | 2026-06-08 | Low | Settings rendered a compact MVP summary instead of the Claude design modal, missing the left section nav, editable profile pane, SSO provider rows, active session, and sticky save bar. | Fixed | Rebuilt the shared settings modal with Radix Dialog/Tabs, local draft-only profile fields, Claude-matched Security & SSO rows, and focused modal/shell tests. |
| B-013 | 2026-06-08 | Low | Settings typography still rendered too large because the modal used generic Tailwind text utilities and one-off arbitrary sizes instead of compact settings-specific type roles. | Fixed | Added shared `pb-settings-*` typography globals, moved the modal onto those classes, and updated tests/rules to prevent future screenshot-to-generic-type drift. |
| B-014 | 2026-06-08 | Low | Settings modal kept a fixed 660px height after the compact typography fix, leaving a large empty area below the profile fields and save bar. | Fixed | Changed the dialog frame to content-fit height with a viewport max-height guard and added a regression assertion against the old fixed-height class. |
| B-015 | 2026-06-08 | Medium | Admin Knowledge base rendered a rushed flat document list/empty placeholder instead of the Claude design collection-first grid and collection detail workflow. | Fixed | Rebuilt the KB admin surface around token-backed global `pb-admin-kb-*` styles, frontend collection grouping, collection drill-in, document status/actions, upload metadata injection, and focused admin tests. |
| B-016 | 2026-06-08 | Medium | Route and query loading states fell back to plain text or empty aurora/workspace surfaces instead of preserving the Claude-designed auth, chat, and admin layouts with shaped skeletons. | Fixed | Added shared Playbook skeleton primitives, App Router `loading.tsx` fallbacks, first-load skeletons, background refresh indicators, chat pending thinking states, and loader-focused frontend tests. |
| B-017 | 2026-06-08 | Low | The chat route/auth skeleton reused grounded-conversation geometry, including a right-side sources panel and top-bar/thread placeholder, even though the loaded default chat page starts as an empty new-chat workspace. | Fixed | Removed the sources panel from the chat workspace fallback, reshaped the main pane around the empty-chat PlaybookAI lockup and composer footprint, and added regression tests for the no-sources loader state. |
| B-018 | 2026-06-08 | Low | Skeleton loaders over-specified deterministic UI, drawing shimmer bars for known headings, subtitles, helper text, form labels, and input interiors so loading states felt like fake content instead of structural shells. | Fixed | Converted loaders to a structural-only policy: stable chrome renders directly, only data-backed regions shimmer, misleading first-load subtitles are hidden, and route/panel tests assert the absence of nonessential skeleton content. |
| B-019 | 2026-06-08 | Low | The chat route/auth skeleton hand-rolled the rail and composer chrome, causing visual drift from the loaded chat page: the New chat action was not a real button shape, the Ask action looked orange/enabled, and the composer missed the persistent AI disclaimer that defines the bottom spacing. | Fixed | Aligned the chat skeleton with the loaded rail/composer structure, using disabled real controls, the shared disabled button styling, an empty disabled textarea, and the stable AI disclaimer while keeping conversation/user data skeletonized. |
| B-020 | 2026-06-12 | High | KB vector search could return chunks for documents whose ingestion had not reached the ready/success state because search read vector rows without joining the document lifecycle table. | Fixed | Added a success-only `kb.documents` join to the shared KB vector search statement so failed, in-progress, and orphaned deleted-document vectors are excluded before results are returned. |
| B-021 | 2026-06-12 | Medium | KB ingestion could silently continue when parsing or chunking produced no usable text, leaving documents without retrievable chunks instead of a clear failed status. | Fixed | Added the internal `NO_TEXT_EXTRACTED` failure path across parse, chunk, and defensive embed stages, with status/webhook propagation and regression tests. |
| B-022 | 2026-06-14 | Medium | KB OCR was still stubbed, so high-complexity scanned PDFs could not use the product-approved LiteLLM vision path. | Fixed | Added opt-in `OCR_PROVIDER=vlm` scanned PDF OCR through the LiteLLM VLM alias, kept Textract unsupported, and documented the new env settings and fallback behavior. |
| B-023 | 2026-06-15 | High | Direct KB-service ingests tried to call the backend status webhook even when no backend-owned document existed, and permanent worker failures could render exception locals containing secrets. | Fixed | Made webhook delivery opt-in per ingest, kept backend-owned webhooks canonical, and added centralized pre-render redaction for structlog, stdlib logging, settings reprs, and worker dead-letter logs. |
| B-024 | 2026-06-17 | Low | KB-service host-run Uvicorn access logging emits formatter errors (`not enough values to unpack`) under the current logging configuration, creating noisy local smoke logs even when requests succeed. | Open | Reconcile Uvicorn access-log formatting with the shared structlog configuration or disable access logs for the host-run command. |
| B-025 | 2026-06-18 | Medium | Backend stdlib secret redaction pre-rendered log messages and cleared lazy `LogRecord.args`, causing Uvicorn access logs to emit formatter errors after normal requests. | Fixed | Redacted log message templates and args independently, preserved formatter argument shape, and added Uvicorn access-log plus sensitive query-param regression tests. |
| B-026 | 2026-06-18 | Medium | Local Infinity reranker startup could run the amd64 CPU image under Rosetta, fail before readiness, and restart indefinitely while consuming CPU and memory. | Fixed | Removed the shared amd64 platform pin, pinned local reranker to an ARM64-compatible Infinity tag, disabled local reranker restarts, split reranker env from LiteLLM provider secrets, and documented one-time container cleanup. |
| B-027 | 2026-06-19 | Medium | The ARM64 local reranker path still OOM-killed on 8 GB Apple Silicon while optimizing `BAAI/bge-reranker-base` with the `optimum` engine. | Fixed | Overrode only local Compose to serve `mixedbread-ai/mxbai-rerank-xsmall-v1` with Infinity's `torch` engine while leaving dev/prod on the base BAAI target. |
